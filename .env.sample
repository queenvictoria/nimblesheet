# Path to the Nimble Vault docs directory containing the Classes folder.
# Defaults to docs/Nimble Vault (relative to the project root) if not set.
# Override when the vault lives outside the repo, e.g. a shared Obsidian vault.
# NIMBLE_DOCS_DIR=/path/to/Nimble Vault
